# Copy to .env.local and fill in your own key.
# Finnhub market-data API key — https://finnhub.io/dashboard
# Used only for the local-dev frontend-direct spike (Phase 4). Never deploy
# this to a browser build; it moves server-side (BFF) in Phase 6.
VITE_FINNHUB_API_KEY=
